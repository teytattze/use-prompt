{
  "name": "@use-prompt/service",
  "type": "module",
  "scripts": {
    "build": "bun build src/api.ts --outdir ./dist --target bun",
    "check-types": "tsc --noEmit --skipLibCheck",
    "dev": "bun --watch src/api.ts",
    "dev:worker": "bun --watch src/worker.ts",
    "lint": "eslint",
    "start": "NODE_ENV=production bun dist/api.js",
    "start:worker": "NODE_ENV=production bun src/worker.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@elysiajs/openapi": "catalog:",
    "elysia": "catalog:",
    "es-toolkit": "catalog:",
    "jose": "catalog:",
    "mongodb": "catalog:",
    "neverthrow": "catalog:",
    "pino": "catalog:",
    "ulid": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@total-typescript/ts-reset": "catalog:",
    "@use-prompt/eslint-config": "workspace:*",
    "@use-prompt/typescript-config": "workspace:*",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "eslint": "catalog:"
  }
}
